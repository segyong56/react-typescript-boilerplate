import React, { FC } from 'react';
import { Layout, Title } from '../../components';

const AboutPage: FC = () => {
  return (
    <Layout>
      <Title text="About" />
    </Layout>
  );
};

export default AboutPage;
