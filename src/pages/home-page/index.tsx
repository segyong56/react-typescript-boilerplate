import React, { FC } from 'react';
import { Layout, Title } from '../../components';

const HomePage: FC = () => {
  return (
    <Layout>
      <Title text="Home" />
    </Layout>
  );
};

export default HomePage;
